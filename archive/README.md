# Archive

This directory holds non-runtime items that were removed from the repo root to reduce confusion.

- `web/`
  - Historical placeholder for a Lovable export. Not an active app.
- `supabase/`
  - Temp-only Supabase CLI metadata that should not be treated as infra source of truth.

Archived items are kept for reference only unless they are intentionally restored.
