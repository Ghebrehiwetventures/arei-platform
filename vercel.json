{
  "buildCommand": "cd packages/arei-sdk && npm install && npm run build && cd ../kaza-verde && npm install && npm run build",
  "outputDirectory": "kaza-verde/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
